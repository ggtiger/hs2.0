# hs2.0 服务器部署与修复记录

- **日期**：2026-08-25
- **服务器**：TencentOS 4（VM-0-9-tencentos，内网 IP 172.21.0.9）
- **部署目录**：`/opt/realso/hs2.0`（源码仓库 + 构建产物 + 部署配置）

---

## 一、最终运行状态

| 服务 | 容器 | 访问地址 | 说明 |
|------|------|---------|------|
| 前端管理后台 | `realso-admin` | `http://<主机>:8080` | nginx 静态资源 + API 反代 |
| 认证服务 | `realso-auth` | `:5000`（宿主映射，兼容保留） | IdentityServer4，经 8080 `/auth/` 反代 |
| 主 API | `realso-webapi` | `:5001`（宿主映射，兼容保留） | 经 8080 `/api/` 反代 |
| MySQL | `realso-mysql` | `:13306`（宿主映射） | root / <数据库密码>，库 d0001 |
| OnlyOffice | `onlyoffice-document-server` | `:8088` | 已接入 `realso_default` 网络 |

**登录账号**：admin（王虎 / GJ001，超级管理员），密码见 .env / 由管理员保管

**MySQL 关键参数**：`lower_case_table_names=1`、`sql_mode` 不含 `ONLY_FULL_GROUP_BY`

---

## 二、部署架构要点

- 部署基于仓库 `deploy/` 目录：`docker-compose.yml` + `.env` + `docker-compose.override.yml`
- 前端经 nginx（8080）反代：`/api/` → webapi:5001、`/auth/` → auth:5000（剥 `/auth` 前缀）、`/hub/`、`/assistantHub` → webapi WebSocket
- MySQL / upload / log 数据卷（`realso_mysql_data` 等）沿用旧部署，未重建（仅因 `lower_case_table_names` 重建过一次，见下文）
- 镜像均为本机从源码构建：`realso-webapi` / `realso-auth`（Debian Buster + .NET 2.2.8 运行时）、`realso-admin`（nginx:alpine）
- Docker 镜像加速源：`/etc/docker/daemon.json` → `mirror.ccs.tencentyun.com`、`docker.1panel.live`、`dockerproxy.net`

---

## 三、代码/配置修改清单

### 前端 p-admin

| 文件 | 修改 | 原因 |
|------|------|------|
| `src/api/urls.js` | API 地址由硬编码 `http://127.0.0.1:5001/5000` 改为相对路径（`''`、`/auth`、`/api/file/` 等），走 8080 nginx 反代 | 浏览器打开部署前端时，JS 请求发到了访问者自己的 localhost |
| `src/components/rs-onlyoffice-shared.js` | `ONLYOFFICE_URL` 改为动态：`浏览器当前主机 + :8088` | 原写死 `localhost:8088`，浏览器会去访问者本机加载 OnlyOffice API |
| `src/components/rs-onlyoffice-shared.js` | 动态端口改为「浏览器端口 + 8」（6080→6088 / 8080→8088），无端口回退 6088 | 外网代理切换后 OnlyOffice 端口变为 6088 |
| `src/components/rs-onlyoffice-preview/index.vue` | 文件预览下载地址改为动态绝对地址：`浏览器主机 + :6001` + `/api/file/{id}` | 旧代码把相对路径拼成 `host.docker.internal`（Docker Desktop 时代逻辑），预览报"下载失败" |
| `config/prod.env.js` | `ONLYOFFICE_DOC_SERVER` 置空（触发上述动态回退） | 同上 |
| `src/pages/cgdd/components/neirong.vue` | import 路径 `./addXm` → `./addXM` | Linux 文件名大小写敏感，原代码无法在 Linux 构建 |
| `src/components/main copy.vue` | import 路径 `./views/home-Body` → `./views/home-body` | 同上（该文件是误留的副本，但会被自动扫描编译） |

### 部署配置 deploy/

| 文件 | 修改 | 原因 |
|------|------|------|
| `docker-compose.yml` | mysql 启动参数增加 `--lower_case_table_names=1` | 代码查询大写表名（`TSS_RESOURCE`），Linux MySQL 默认大小写敏感导致"表不存在" |
| `docker-compose.yml` | mysql 启动参数增加 `--sql-mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION`（即去掉 `ONLY_FULL_GROUP_BY`） | 系统 SQL 为旧版 MySQL 风格，GROUP BY 报错 |
| `docker-compose.yml` | webapi 环境变量增加 `Auth__Issuer: "http://localhost:5000"` | 见下文 JWT 修复 |
| `nginx.conf` | `/api/` 的 `proxy_pass` 去掉 URI 部分（由 `http://webapi:5001/` 改为 `http://webapi:5001`），完整保留 `/api` 前缀 | 后端路由为 `api/[controller]`，原配置剥掉前缀导致 404 |
| `nginx.conf` | 新增 `/assistantHub` WebSocket 反代 | AI 助手 SignalR 路径此前无转发 |
| `nginx.conf` | `gzip_comp_level 6` + `gzip_vary on` | 默认 1 级压缩率低，6 级可再省 ~15% 体积 |
| `nginx.conf` | `location ~* \.map$ { return 404; }` | 生产环境不对外提供 source map（vendor.js.map 达 29MB） |
| `.env`（新建） | `MYSQL_ROOT_PASSWORD=<数据库密码>`、`MYSQL_DATABASE=d0001`、`MYSQL_PORT=13306`、`ADMIN_PORT=8080` | 生产环境变量 |
| `docker-compose.override.yml`（新建） | 保留 auth 5000 / webapi 5001 的宿主机端口映射 | 旧客户端（移动端/外部调用）兼容；删除此文件则仅 8080 对外 |
| `docker-compose.yml` | auth/webapi 的 upload、log 由命名卷改为宿主机目录映射（`/opt/realso/upload`、`/opt/realso/log`） | 文件内容直接落在本地文件夹，便于直接访问管理；原卷数据已迁移 |
| `nginx-proxy-external.conf`（新建） | 外网代理服务器 nginx 配置（6080/6088/6001/6000 → 内网 8080/8088/5001/5000），含 WebSocket 升级与长超时 | 本服务器无公网，由 101.42.97.106 反代 |

### 后端 netcore

| 文件 | 修改 | 原因 |
|------|------|------|
| `Realso.WebAPI/Startup.cs` | JwtBearer 增加 `TokenValidationParameters.ValidIssuer = Configuration["Auth:Issuer"] ?? "http://localhost:5000"` | 见下文 JWT 修复 |
| `Realso.WebAPI/appsettings.json` | `OnlyOffice:DocServerUrl` → `http://onlyoffice-document-server`；`OnlyOffice:ApiUrl` → 先改为容器名 `http://webapi:5001`，外网化后**最终为 `http://101.42.97.106:6001`** | 原值在 Linux 容器内不可达；公网地址需同时满足浏览器侧与 docservice 侧可达（本机经 VPC 内路由可访问该公网 IP） |

| `Realso.Auth/Startup.cs` | 签名密钥改为从挂载文件 `/app/signing-key.json` 加载（宿主机 `/opt/realso/auth-signing-key.json`，JSON 格式 RSAParameters） | 原 `RSA.Create(2048)` 每次启动生成新密钥：重启后所有已签发 token 失效，且 webapi 缓存旧公钥导致 `signature is invalid` 401 |
| `docker-compose.yml` | auth 服务挂载签名密钥文件 | 同上 |
| 数据库 `tss_resfilter` | `VSS_USER` 的 `F02` 模板去掉 `A.PASSWORD=@PASSWORD` 条件（密码校验已由新代码在应用层完成） | 上游新登录代码不再传 PASSWORD 参数 → `Parameter '@PASSWORD' must be defined` |
| 数据库 `tss_user` | `PASSWORD` 字段 `varchar(64)` → `varchar(200)` | 新代码将明文密码升级为 89 字符哈希（盐24+$+SHA256×64），超出原字段长度 |

> 说明：`Realso.Auth` 的 `UserController.cs` 与 `Startup.cs` 排查过程中曾修改，最终已还原为原样，无净改动。

### OnlyOffice 文档服务器（容器内配置，非仓库文件）

| 项 | 修改 | 原因 |
|----|------|------|
| 网络 | 加入 `realso_default` 网络 | 容器间按名字互通 |
| `local.json` → `token.enable` | browser / request.inbox / request.outbox 全部 `false` | 后端无 JWT 签名能力，否则报"文档安全令牌的格式不正确" |
| `local.json` → `request-filtering-agent` | `allowPrivateIPAddress: true`、`allowMetaIPAddress: true` | 默认 SSRF 防护拒绝下载私有 IP（webapi=172.19.0.4），报"下载失败" |
| 原配置备份 | `/tmp/oo-local.json.bak` | — |
| `/etc/nginx/includes/http-common.conf`（容器内） | `proxy_set_header Host "101.42.97.106:6088"`；`$the_host` map 固定为 `"101.42.97.106:6088"` | 代理服务器未正确传递 Host/X-Forwarded-Host，docservice 按 XFH 生成缓存 URL 变成 `hs2_backend_onlyoffice`，浏览器 ERR_NAME_NOT_RESOLVED。注意：实际生效文件是 `/etc/nginx/includes/`（非 `/etc/onlyoffice/documentserver/nginx/includes/` 模板树） |
| `local.json` → `token.enable`（二次） | JWT 曾被某操作还原为开启（"文档安全令牌"错误复现），已再次关闭 | 若第三次复现需排查是什么在重置该配置 |
| 容器重建（三次/根治） | 用官方环境变量 `-e JWT_ENABLED=false` 重建容器（数据 bind 挂载原样保留，容器内修复全部重放） | 根因：DS 入口脚本 `run-document-server.sh` 每次容器启动都把 JWT 开关按 `${JWT_ENABLED:-true}` 写回 local.json，重启 docker 即复现；重建后重启验证通过，永久生效 |
| `sdkjs-plugins/fieldinserter`（容器内） | 部署插件三件套（config.json/index.html/index.js）+ gzip；index.js 的 webapi 端口改为 6001 | 插件原本未部署；fallback 地址 `localhost:5001` 在浏览器环境错误 |

---

## 四、线上问题排查与修复过程（按时间线）

1. **登录 500 — `Table 'd0001.TSS_RESOURCE' doesn't exist`**
   - 现象：数据库已导入但登录报错。
   - 根因①：导入的表为小写 `tss_resource`，代码查询大写 `TSS_RESOURCE`，MySQL 容器 `lower_case_table_names=0`（Linux 默认，大小写敏感）。系统按 Windows 环境（不区分大小写）开发。
   - 根因②（后续）：登录接口要求表单值先 JSON 序列化（前端 axios `tran` 转换），直接发裸文本会 500——curl 测试方式问题，非系统 bug。
   - 修复：`--lower_case_table_names=1`；因该参数初始化后不可改，备份全库（`/opt/realso/d0001-backup-20260825.sql`，450MB/92 表）→ 重建数据卷 → 自动回导。

2. **登录成功但业务接口 401**
   - 根因：Auth 在容器内以 `http://localhost:5000` 请求 IdentityServer 签发 token（`iss=http://localhost:5000`），而 webapi 校验 Authority 为 `http://auth:5000`，discovery 文档 issuer 与 token iss 不一致。
   - 尝试过的方案：设 `IssuerUri=http://auth:5000`——失败，IdentityModel 客户端要求 authority 与 discovery issuer 完全一致且非 loopback 强制 HTTPS。
   - 最终修复：保持签发链路不变，webapi 侧显式 `ValidIssuer=http://localhost:5000`（compose 可配 `Auth__Issuer`）。

3. **Word 模版编辑器 404 / OnlyOffice API 加载失败**
   - 根因：前端构建时写死 `localhost:8088`，浏览器从访问者本机加载 OnlyOffice API。
   - 修复：前端动态取「浏览器主机 + :8088」；OnlyOffice 容器接入 realso 网络；appsettings 两处 URL 改为容器名。
   - 注意：数据库中 2022–2024 年的历史文件记录在服务器上无物理文件，打开报"文件不存在于磁盘"属正常；只有新上传的文件可在线编辑。

4. **编辑器报"文档安全令牌的格式不正确"**
   - 根因：OnlyOffice 服务器 JWT 校验三层全开，后端无 token 生成逻辑。
   - 修复：关闭 DS 的 JWT（browser/inbox/outbox）。如需恢复，需后端用 DS secret `<DS-secret>` 对编辑器配置签名。

5. **编辑器报"下载失败"**
   - 根因：OnlyOffice 内置 SSRF 防护拒绝下载私有 IP（`webapi` 解析为 172.19.0.4）。
   - 修复：`request-filtering-agent.allowPrivateIPAddress=true`，并用 DS 转换服务实测通过（FileResult 返回成功）。

6. **GROUP BY 报错（`only_full_group_by`）**
   - 根因：MySQL 8.0 默认严格模式，系统 SQL 为旧版风格。
   - 修复：全局及 compose 启动参数移除 `ONLY_FULL_GROUP_BY`。

7. **切换外网代理访问（101.42.97.106）**
   - 本服务器无公网/不能访问外网，由代理服务器（同 VPC，内网 172.21.0.8）反代。
   - 对外端口：6080（后台）/6088（OnlyOffice）/6001（API）/6000（认证）；本机端口映射不变。
   - 前端 OnlyOffice 端口改为动态「浏览器端口+8」；插件 webapi 端口改 6001；`OnlyOffice:ApiUrl` 改公网地址。
   - 关键验证：本机经 VPC 内路由可直连代理公网 IP（3.5ms），因此公网地址对 docservice 同样可达。

8. **编辑器缓存文件 URL 错误（`ERR_NAME_NOT_RESOLVED: hs2_backend_onlyoffice`）**
   - 根因：代理服务器的 nginx 未正确设置 `proxy_set_header Host`（nginx 默认传 upstream 名），DS 的 `$the_host` map 优先取 `X-Forwarded-Host`，导致 docservice 生成的缓存文件 URL 主机名变成 upstream 名。
   - 排查要点：docservice 生成的 URL 反映它收到的头；曾误改 `/etc/onlyoffice/documentserver/nginx/includes/`（模板树），实际生效的是 `/etc/nginx/includes/`。
   - 修复：在 DS 容器内 `/etc/nginx/includes/http-common.conf` 固定 `Host` 头与 `$the_host` map 为公网地址，一劳永逸（代理侧配置问题依旧存在但不再影响）。

9. **外网模式编辑器"下载失败"（document.url 内网域名）**
   - 根因：编辑器配置的 `document.url` 为 docker 内网域名 `webapi:5001`，DS 9.4 前端在浏览器侧校验该 URL，用户浏览器无法解析 → 失败（docservice 日志无新记录即为浏览器侧失败的特征）。
   - 修复：`OnlyOffice:ApiUrl` 改为公网 `http://101.42.97.106:6001`，浏览器与 docservice 双侧实测 200。

10. **"文档安全令牌的格式不正确"复现（第二次）**
    - 根因：DS 容器 `local.json` 的 JWT 三开关被某操作还原为开启（容器未重建，其余修复均在）。
    - 修复：再次关闭并重启 DS 服务；提醒若第三次出现需排查重置源头。

11. **文件预览"下载失败"**
    - 根因：`rs-onlyoffice-preview/index.vue` 用旧逻辑 `replace('localhost','host.docker.internal')` 拼下载地址，API 地址改相对路径后拼出相对 URL，DS 无法下载。
    - 修复：改为动态绝对地址（浏览器主机 + 6001 + `/api/file/{id}`）；`/api/file/{id}` 无鉴权（密码保护证书除外），两侧实测 200。

13. **JWT 反复重置（第三次，"文档安全令牌"随 docker 重启复现）**
    - 根因：DS 容器入口脚本每次启动按 `JWT_ENABLED`（默认 true）重写 local.json。
    - 根治：`-e JWT_ENABLED=false` 重建容器；重启验证通过。

14. **登录成功但业务接口 401（`signature is invalid`）**
    - 根因①：Auth 每次启动随机生成 RSA 签名密钥，重启即导致所有已签发 token 失效、webapi 缓存的公钥不匹配。
    - 根因②：上游新登录代码不再传 PASSWORD 参数，而 DB 的 F02 模板仍含 `@PASSWORD` → `Parameter '@PASSWORD' must be defined`。
    - 根因③：新代码把明文密码升级为 89 字符哈希，超出 `PASSWORD` varchar(64) → `Data too long`。
    - 修复：持久化签名密钥（`/opt/realso/auth-signing-key.json` 挂载 + `ImportParameters` 加载）；DB 模板去 PASSWORD 条件；字段扩至 varchar(200)。重启 auth 后旧 token 仍有效（实测通过）。

12. **前端 vendor.js 下载慢（3MB/25s）**
    - 排查：内网直连与代理内网转发均 0.14s，慢在公网路径；本机公网出口 1M、代理 5M、办公室网络正常。
    - 已做优化：gzip 级别 1→6（3.1MB→2.64MB）、禁止 source map（-29MB）。
    - 结论：用户侧实测仍慢时，优先检查代理服务器实时带宽占用与端到端链路（手机热点对比测试可定位）。

---

## 五、构建环境（本机留存）

| 项 | 位置/值 |
|----|---------|
| .NET SDK 2.2.402 | `/opt/dotnet`（使用需 `PATH=/opt/dotnet:$PATH DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1`） |
| OpenSSL 1.1 兼容库 | `dnf install compat-openssl11`（SDK 2.2 不支持 OpenSSL 3） |
| .NET 2.2.8 运行时（镜像内） | `netcore/runtime/dotnet` |
| publish 产物 | `netcore/publish/{webapi,auth}` |
| 前端构建 | `p-admin/dist`（Node 22 需 `NODE_OPTIONS=--openssl-legacy-provider`，npm 源 npmmirror） |

**重新构建命令**（在服务器上）：

```bash
# 后端
cd /opt/realso/hs2.0/netcore
export PATH=/opt/dotnet:$PATH DOTNET_ROOT=/opt/dotnet DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1
dotnet publish <项目绝对路径> -c Release -r linux-x64 -o ./publish/<webapi|auth> --self-contained false
docker build --platform linux/amd64 -f Dockerfile.local --build-arg APP=<webapi|auth> -t realso-<webapi|auth>:latest .

# 前端
cd /opt/realso/hs2.0/p-admin
NODE_OPTIONS=--openssl-legacy-provider npm run build
cd /opt/realso/hs2.0
docker build --platform linux/amd64 -f deploy/Dockerfile.p-admin -t realso-admin:latest .

# 部署
cd /opt/realso/hs2.0/deploy
docker compose -p realso up -d
```

> 注意：`dotnet publish` 的 `-o` 相对路径会解析到项目目录下，构建前需把产物移到 `netcore/publish/`（Dockerfile 构建上下文要求）。

---

## 六、运维速查

```bash
cd /opt/realso/hs2.0/deploy
docker compose -p realso ps                      # 查看状态
docker compose -p realso logs -f webapi          # 查看日志
docker compose -p realso restart auth webapi     # 重启应用
docker compose -p realso down                    # 停止（不含 onlyoffice）

# 数据库
mysql -h 127.0.0.1 -P 13306 -uroot -p<数据库密码> d0001

# 数据库备份（全库）
docker exec realso-mysql mysqldump -uroot -p<数据库密码> --single-transaction --routines --triggers --events d0001 > d0001-backup.sql
```

**安全组/防火墙**（如从外网访问）：放行 8080（后台）、8088（OnlyOffice）、13306（MySQL，按需）、5000/5001（按需，已保留宿主映射）。

**OnlyOffice 容器侧修复清单**（容器重建或异常后需复查，均在 `onlyoffice-document-server` 容器内）：
```bash
# 1. JWT 关闭（"文档安全令牌"报错时查）
cat /etc/onlyoffice/documentserver/local.json | python3 -c "import sys,json;print(json.load(sys.stdin)['services']['CoAuthoring']['token']['enable'])"
# 2. 私有 IP 放行（"下载失败"时查）
grep -A3 request-filtering-agent /etc/onlyoffice/documentserver/local.json
# 3. 缓存 URL 固定 Host（ERR_NAME_NOT_RESOLVED 时查）
grep -n '101.42.97.106' /etc/nginx/includes/http-common.conf
# 4. 插件部署（工具栏"字段插入"缺失时查）
ls /var/www/onlyoffice/documentserver/sdkjs-plugins/fieldinserter/
# 5. 网络（webapi/onlyoffice 互通异常时查）
docker inspect onlyoffice-document-server --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}'
```

---

## 七、遗留事项与建议

1. **本次全部源码修改尚未提交 git**。建议合回代码库的文件：
   - `p-admin/src/api/urls.js`、`p-admin/src/components/rs-onlyoffice-shared.js`、`p-admin/config/prod.env.js`
   - `p-admin/src/components/rs-onlyoffice-preview/index.vue`（预览 URL）
   - `p-admin/src/pages/cgdd/components/neirong.vue`、`p-admin/src/components/main copy.vue`（大小写）
   - `deploy/nginx.conf`、`deploy/docker-compose.yml`、`deploy/docker-compose.override.yml`、`deploy/.env`（.env 建议不入库，仅作模板）
   - `deploy/nginx-proxy-external.conf`（外网代理配置）
   - `onlyoffice-plugins/fieldinserter/index.js`（端口 6001）
   - `netcore/Realso.WebAPI/Startup.cs`（ValidIssuer）、`netcore/Realso.WebAPI/appsettings.json`（OnlyOffice 地址）
2. `urls.js` 中 `socket` 类型仍指向外部 `ws://ydzl.gujing.net:9091`，未处理。
3. OnlyOffice JWT 已关闭（安全性与便利性的取舍）；如需启用需后端实现 token 签名。
4. `appsettings.json` 中的 `Upload:ROOT`（开发机路径）由 compose 环境变量 `/app/upload/` 覆盖，容器内生效。
5. 仓库内 `sql/aidev/` 为增量脚本，基础 schema 与种子数据以生产库 dump 为准；建议把 `/opt/realso/d0001-backup-20260825.sql` 妥善保存（或转存到版本库/对象存储）。

---

## 八、外网代理访问模式（补充）

- 外网入口：`101.42.97.106`（与内网服务器同 VPC，内网 IP 172.21.0.8；本服务器可经 VPC 路由直接访问其公网 IP）
- 端口映射（外网 → 内网）：`6080→8080`（后台）、`6088→8088`（OnlyOffice）、`6001→5001`（API）、`6000→5000`（认证）
- 代理配置：`deploy/nginx-proxy-external.conf`（部署于代理服务器 `/etc/nginx/conf.d/hs2.0-proxy.conf`）
- 前端 OnlyOffice 地址动态推导：浏览器端口 +8（6080→6088 / 8080→8088）
- 插件 fieldinserter 的 webapi 端口：6001（外网模式）
- **`Realso.WebAPI/appsettings.json` → `OnlyOffice:ApiUrl` = `http://101.42.97.106:6001`**：
  - 原因：DS 9.4 前端在浏览器侧校验 `document.url`，原 docker 内网域名 `webapi:5001` 浏览器无法解析 → 「下载失败」
  - 该公网地址经 VPC 内路由，本服务器（docservice 下载/回调）同样可达（实测 3.5ms）
  - 代理服务器公网 IP 变更时需同步修改并重新构建 webapi 镜像
