# 开发环境配置
# H5 端走 vite.config.js 双代理（/api/user→5000, /api→5001），忽略此处的 base
# 小程序/App 端直连后端，需配置完整 URL（局域网调试把 localhost 改为后端机 IP）
#   微信开发者工具需勾选「不校验合法域名」
VITE_DATA_BASE=http://localhost:5001
VITE_AUTH_BASE=http://localhost:5000
